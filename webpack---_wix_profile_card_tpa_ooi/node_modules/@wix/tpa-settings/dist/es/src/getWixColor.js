import * as parseColor from 'color';
export function getWixColor(_a) {
    var colors = _a.colors,
        reference = _a.reference,
        _b = _a.opacity,
        opacity = _b === void 0 ? 1 : _b;
    var wixColor = colors.find(function(c) {
        return c.reference === reference;
    });
    var parsedColor = parseColor(wixColor.value);
    return {
        opacity: opacity,
        value: parsedColor
            .rgb()
            .alpha(opacity)
            .string(),
        name: wixColor.name,
    };
}
export var wixColorParam = function(name, opacity) {
    return function(_a) {
        var colors = _a.colors;
        return getWixColor({
            reference: name,
            opacity: opacity,
            colors: colors
        });
    };
};
//# sourceMappingURL=getWixColor.js.map