import {
    __spreadArray
} from "tslib";
import {
    OfferedAsType
} from '@wix/bookings-uou-types';
export function determinePaymentOptionsBy(pricingPlans, paymentOptions) {
    return __spreadArray(__spreadArray([], insertIf((pricingPlans === null || pricingPlans === void 0 ? void 0 : pricingPlans.length) && paymentOptions.wixPaidPlan, OfferedAsType.PRICING_PLAN), true), insertIf(isPayable(paymentOptions.wixPayInPerson, paymentOptions.wixPayOnline), OfferedAsType.ONE_TIME), true);
}
export function mapServiceOfferedAsDto(serviceResponse) {
    var paymentOptions = serviceResponse.service.paymentOptions;
    return determinePaymentOptionsBy(serviceResponse.pricingPlans, paymentOptions);
}

function insertIf(condition) {
    var elements = [];
    for (var _i = 1; _i < arguments.length; _i++) {
        elements[_i - 1] = arguments[_i];
    }
    return condition ? elements : [];
}

function isPayable(paidOffline, paidOnline) {
    return paidOffline || paidOnline;
}
//# sourceMappingURL=service-offered-as.mapper.js.map