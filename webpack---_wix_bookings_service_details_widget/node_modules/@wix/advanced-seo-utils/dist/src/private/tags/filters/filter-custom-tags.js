"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.filterCustomTags = void 0;
var safely_parse_json_data_1 = require("../../renderer/utils/safely-parse-json-data");
exports.filterCustomTags = function(advancedSeoData, _a) {
    var _b = (_a === void 0 ? {} : _a).keepDefaultTags,
        keepDefaultTags = _b === void 0 ? false : _b;
    var filteredTags;
    if (typeof advancedSeoData === 'string') {
        filteredTags = safely_parse_json_data_1.safelyParseJsonData(advancedSeoData);
    }
    filteredTags = ((filteredTags || advancedSeoData).tags || []).filter(function(tag) {
        return keepDefaultTags ? !tag.custom || tag.isDefault : !tag.custom;
    });
    return {
        tags: filteredTags
    };
};