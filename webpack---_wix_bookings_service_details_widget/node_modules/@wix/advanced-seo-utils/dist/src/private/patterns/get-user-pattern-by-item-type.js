"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.getUserPatternByItemType = void 0;
var ItemTypes_1 = require("../types/ItemTypes");

function getUserPatternByItemType(userPatternsArray, itemType, _a) {
    if (userPatternsArray === void 0) {
        userPatternsArray = [];
    }
    if (itemType === void 0) {
        itemType = ItemTypes_1.STATIC_PAGE_V2;
    }
    var _b = (_a === void 0 ? {} : _a).logError,
        logError = _b === void 0 ? function() {} : _b;
    var userPatternContent;
    try {
        userPatternContent =
            (userPatternsArray.find(function(_a) {
                var patternType = _a.patternType;
                return patternType === itemType;
            }) || {}).content || {};
        if (typeof userPatternContent === 'string') {
            return JSON.parse(userPatternContent);
        }
        return userPatternContent;
    } catch (error) {
        logError({
            error: error,
            data: {
                value: userPatternContent
            }
        });
        return {};
    }
}
exports.getUserPatternByItemType = getUserPatternByItemType;