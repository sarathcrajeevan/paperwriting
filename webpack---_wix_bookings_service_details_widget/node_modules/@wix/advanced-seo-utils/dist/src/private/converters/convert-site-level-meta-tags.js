"use strict";
var __importDefault = (this && this.__importDefault) || function(mod) {
    return (mod && mod.__esModule) ? mod : {
        "default": mod
    };
};
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.convertSiteLevelMetaTags = void 0;
var TagTypes_1 = require("../types/TagTypes");
var Props_1 = __importDefault(require("../types/Props"));

function convertSiteLevelMetaTags(payload) {
    var result = {
        tags: [],
    };
    if (payload && Array.isArray(payload)) {
        result.tags = payload.reduce(function(acc, curr) {
            var _a;
            if (curr && curr.name && curr.value) {
                return acc.concat({
                    type: TagTypes_1.TAG_TYPES.META,
                    props: (_a = {},
                        _a[curr.name.startsWith('og:') ?
                            Props_1.default.PROPERTY :
                            Props_1.default.NAME] = curr.name,
                        _a.content = curr.value,
                        _a),
                });
            }
            return acc;
        }, result.tags);
    }
    return result;
}
exports.convertSiteLevelMetaTags = convertSiteLevelMetaTags;