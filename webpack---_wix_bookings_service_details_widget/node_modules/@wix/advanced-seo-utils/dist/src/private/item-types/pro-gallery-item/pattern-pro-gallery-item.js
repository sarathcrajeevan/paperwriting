"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function() {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.PRO_GALLERY_ITEM = void 0;
var builders_1 = require("../../types/builders");
exports.PRO_GALLERY_ITEM = {
    tags: __spreadArrays([
        builders_1.buildTitle('{{item.title}} | {{page.name}}'),
        builders_1.buildStandardMeta('description', "{{item.description}}")
    ], builders_1.buildOgMetaRefs(), [
        builders_1.buildOgMeta('og:url', '{{page.fullScreenUrl}}'),
        builders_1.buildOgMeta('og:site_name', '{{site.name}}'),
        builders_1.buildOgMeta('og:type', 'website'),
        builders_1.buildOgMeta('og:image', '{{item.image.url}}'),
        builders_1.buildOgMeta('og:image:width', '{{item.image.width}}'),
        builders_1.buildOgMeta('og:image:height', '{{item.image.height}}'),
        builders_1.buildLink({
            rel: 'canonical',
            href: '{{page.url}}'
        }),
        builders_1.buildStandardMeta('robots', 'index')
    ], builders_1.buildTwitterMetaRefs()),
};