"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function() {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.BOOKINGS_FORM = void 0;
var utils_1 = require("../../adapters/utils");
var builders_1 = require("../../types/builders");
var adapter_bookings_form_1 = require("./adapter-bookings-form");
var SITE_NAME = utils_1.siteIDs.SITE_NAME;
var FORM_NAME = adapter_bookings_form_1.IDs.FORM_NAME,
    FORM_DESCRIPTION = adapter_bookings_form_1.IDs.FORM_DESCRIPTION,
    PAGE_URL = adapter_bookings_form_1.IDs.PAGE_URL;
exports.BOOKINGS_FORM = {
    tags: __spreadArrays([
        builders_1.buildTitle("{{" + FORM_NAME + "}} - {{" + SITE_NAME + "}}"),
        builders_1.buildStandardMeta('description', "{{" + FORM_DESCRIPTION + "}}"),
        builders_1.buildStandardMeta('robots', 'noindex'),
        builders_1.buildLink({
            rel: 'canonical',
            href: "{{" + PAGE_URL + "}}"
        })
    ], builders_1.buildOgMetaRefs(), [
        builders_1.buildOgMeta('og:url', "{{" + PAGE_URL + "}}"),
        builders_1.buildOgMeta('og:site_name', "{{" + SITE_NAME + "}}"),
        builders_1.buildOgMeta('og:type', 'website')
    ], builders_1.buildTwitterMetaRefs()),
};