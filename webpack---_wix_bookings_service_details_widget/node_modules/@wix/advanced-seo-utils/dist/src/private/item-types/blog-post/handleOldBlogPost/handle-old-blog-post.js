"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.handleOldBlog = void 0;
var setters_1 = require("../../../renderer/setters");
var getAmpUrl = function(url) {
    if (url === void 0) {
        url = '';
    }
    return url + (url.includes('?') ? '&' : '?') + '_amp_';
};
exports.handleOldBlog = function(tags, siteLevelData) {
    if (siteLevelData === void 0) {
        siteLevelData = {};
    }
    var _a = siteLevelData.hasBlogAmp,
        hasBlogAmp = _a === void 0 ? false : _a,
        _b = siteLevelData.context,
        _c = (_b === void 0 ? {} : _b).defaultUrl,
        defaultUrl = _c === void 0 ? '' : _c;
    if (hasBlogAmp && defaultUrl.includes('/single-post/')) {
        return setters_1.setLinks(tags, [{
            rel: 'amphtml',
            href: getAmpUrl(defaultUrl),
        }, ]);
    }
    return tags;
};