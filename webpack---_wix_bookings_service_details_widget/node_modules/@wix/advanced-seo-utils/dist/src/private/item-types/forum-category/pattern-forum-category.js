"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function() {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.FORUM_CATEGORY = void 0;
var builders_1 = require("../../types/builders");
exports.FORUM_CATEGORY = {
    tags: __spreadArrays([
        builders_1.buildTitle('{{category.name}} | {{site.name}}'),
        builders_1.buildStandardMeta('description', "{{category.description}}")
    ], builders_1.buildOgMetaRefs(), [
        builders_1.buildOgMeta('og:url', '{{category.url}}'),
        builders_1.buildOgMeta('og:site_name', '{{site.name}}'),
        builders_1.buildOgMeta('og:type', 'website'),
        builders_1.buildOgMeta('og:image', '{{category.image}}'),
        builders_1.buildOgMeta('og:image:height', '{{category.image.height}}'),
        builders_1.buildOgMeta('og:image:width', '{{category.image.width}}'),
        builders_1.buildStandardMeta('robots', 'index')
    ], builders_1.buildTwitterMetaRefs(), [
        builders_1.buildLink({
            rel: 'canonical',
            href: '{{category.url}}'
        }),
    ]),
};