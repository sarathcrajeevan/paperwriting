"use strict";
var __spreadArrays = (this && this.__spreadArrays) || function() {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.STATIC_PAGE = void 0;
var builders_1 = require("../../types/builders");
exports.STATIC_PAGE = {
    tags: __spreadArrays([
        builders_1.buildTitle('{{page.title}}'),
        builders_1.buildStandardMeta('description', "{{page.description}}")
    ], builders_1.buildOgMetaRefs(), [
        builders_1.buildOgMeta('og:url', '{{page.url}}'),
        builders_1.buildOgMeta('og:site_name', '{{site.name}}'),
        builders_1.buildOgMeta('og:type', 'website'),
        builders_1.buildOgMeta('og:image', '{{page.image}}'),
        builders_1.buildOgMeta('og:image:width', '{{page.imageWidth}}'),
        builders_1.buildOgMeta('og:image:height', '{{page.imageHeight}}'),
        builders_1.buildOgMeta('fb:admins', "{{site.facebookAdminId}}"),
        builders_1.buildLink({
            rel: 'canonical',
            href: '{{page.url}}'
        }),
    ]),
};