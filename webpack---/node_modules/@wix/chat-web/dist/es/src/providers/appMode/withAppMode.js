import {
    __assign,
    __extends
} from "tslib";
import React from 'react';
import PropTypes from 'prop-types';
import {
    AppMode
} from '../../constants/app-mode';
var withAppMode = function(ComponentToWrap) {
    var _a;
    return _a = /** @class */ (function(_super) {
            __extends(AppModeComponent, _super);

            function AppModeComponent() {
                return _super !== null && _super.apply(this, arguments) || this;
            }
            AppModeComponent.prototype.render = function() {
                var appMode = this.context.appMode;
                // `as P` can be removed once the following issue closes
                // https://github.com/Microsoft/TypeScript/issues/28884
                var injectedProps = __assign(__assign({}, this.props), {
                    appMode: appMode
                });
                return React.createElement(ComponentToWrap, __assign({}, injectedProps));
            };
            return AppModeComponent;
        }(React.Component)),
        _a.contextTypes = {
            appMode: PropTypes.oneOf([AppMode.FullApp, AppMode.Widget]),
        },
        _a;
};
export default withAppMode;
//# sourceMappingURL=withAppMode.js.map