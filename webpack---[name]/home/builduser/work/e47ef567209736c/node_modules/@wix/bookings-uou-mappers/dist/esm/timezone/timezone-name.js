export function formatTimezone(_a) {
    var _b, _c, _d, _e;
    var date = _a.date,
        timezone = _a.timezone,
        locale = _a.locale,
        // eslint-disable-next-line no-template-curly-in-string
        _f = _a.template,
        // eslint-disable-next-line no-template-curly-in-string
        template = _f === void 0 ? '${timezoneName} (${offset})' : _f;
    var offset = (_c = (_b = Intl.DateTimeFormat(locale, {
            year: 'numeric',
            timeZoneName: 'short',
            timeZone: timezone,
        })
        .formatToParts(date)
        .find(function(part) {
            return part.type === 'timeZoneName';
        })) === null || _b === void 0 ? void 0 : _b.value) !== null && _c !== void 0 ? _c : '';
    var timezoneName = (_e = (_d = Intl.DateTimeFormat(locale, {
            year: 'numeric',
            timeZoneName: 'long',
            timeZone: timezone,
        })
        .formatToParts(date)
        .find(function(part) {
            return part.type === 'timeZoneName';
        })) === null || _d === void 0 ? void 0 : _d.value) !== null && _e !== void 0 ? _e : '';
    return Function('timezoneName', 'offset', "return `" + template + "`;")(timezoneName, offset);
}
//# sourceMappingURL=timezone-name.js.map